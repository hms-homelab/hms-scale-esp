# Triggered by a tag you push by hand, e.g.
#   git tag v2.1.0 && git push origin v2.1.0
# Nothing here tags on its own. The tag must match main/version.h or the run
# fails before anything is published.
name: release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  verify:
    name: verify tag
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.check.outputs.version }}
    steps:
      - uses: actions/checkout@v5

      - name: Tag must match main/version.h
        id: check
        run: |
          tag='${{ github.ref_name }}'
          version=$(sed -n 's/^#define[[:space:]][[:space:]]*HMS_SCALE_ESP_VERSION[[:space:]][[:space:]]*"\([^"]*\)".*/\1/p' main/version.h)
          if [ -z "$version" ]; then
            echo "::error file=main/version.h::could not parse HMS_SCALE_ESP_VERSION"
            exit 1
          fi
          if [ "$tag" != "v$version" ]; then
            echo "::error::tag $tag does not match main/version.h ($version). Bump version.h and retag."
            exit 1
          fi
          if ! grep -qx "## v$version" CHANGELOG.md; then
            echo "::error file=CHANGELOG.md::no '## v$version' section for this release"
            exit 1
          fi
          echo "version=$version" >> "$GITHUB_OUTPUT"

  build:
    needs: verify
    uses: ./.github/workflows/build.yml
    with:
      artifact-name: firmware-release

  publish:
    name: publish release
    needs: [verify, build]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/download-artifact@v7
        with:
          name: firmware-release
          path: dist

      # Pull this version's section out of CHANGELOG.md: everything between its
      # own "## vX.Y.Z" heading and the next "## " heading.
      - name: Build release notes
        env:
          VERSION: ${{ needs.verify.outputs.version }}
        run: |
          awk -v tag="## v$VERSION" '$0 == tag {f=1; next} f && /^## / {exit} f' CHANGELOG.md > notes.md
          {
            echo
            echo "## Install"
            echo
            echo "Updating a board already on v2.0.3 or later: upload \`hms-scale-esp-$VERSION.bin\` on the device's web UI, in the Firmware Update card at \`http://giraffe-scale.local/\`."
            echo
            echo "Blank board, or anything on v2.0.2 or earlier: v2.0.1 and earlier predate the dual-OTA partition table, and v2.0.2's OTA reboots mid-upload, so the first hop has to be over USB."
            echo
            echo '```'
            echo "esptool.py --chip esp32c3 write_flash 0x0 hms-scale-esp-$VERSION-merged.bin"
            echo '```'
            echo
            echo "No credentials are baked in. A board with no saved WiFi comes up as the \`GiraffeScale-XXXX\` open SoftAP at 192.168.4.1 for setup, and NVS survives a reflash, so an already-provisioned board keeps its settings."
          } >> notes.md
          cat notes.md

      - uses: softprops/action-gh-release@v3
        with:
          name: v${{ needs.verify.outputs.version }}
          body_path: notes.md
          files: dist/*
          fail_on_unmatched_files: true
